const SurveyFinishComponent = ({ assignedNumber }: {assignedNumber: number}) => {
  return (
    <div>
      <h2>Your number is {assignedNumber}</h2>
    </div>
  );
};

export default SurveyFinishComponent;